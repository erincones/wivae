import { SourceFilePipe } from './source-file.pipe';

describe('SourceFilePipe', () => {
  it('create an instance', () => {
    const pipe = new SourceFilePipe();
    expect(pipe).toBeTruthy();
  });
});
