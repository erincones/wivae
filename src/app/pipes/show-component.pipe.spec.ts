import { ShowComponentPipe } from './show-component.pipe';

describe('ShowComponentPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowComponentPipe();
    expect(pipe).toBeTruthy();
  });
});
