import { ProgramsCollection } from './programs-collection';

describe('ProgramCollection', () => {
  it('should create an instance', () => {
    expect(new ProgramsCollection()).toBeTruthy();
  });
});
